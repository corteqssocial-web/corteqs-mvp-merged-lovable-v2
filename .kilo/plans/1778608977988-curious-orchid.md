# BÖLÜM L: Supabase Migration Planı

**Devam planı:** `1778584453357-proud-island.md` Bölüm A-K tamamlandı.
**Tarih:** 12 Mayıs 2026
**Öncelik:** Mevcut backend'i koruyarak sadece yeni tablolar ve kolonlar ekleme

---

## Context: Mevcut Durum

Bölüm A-K başarıyla tamamlandı:
- `bun run build` hatasız
- `bun run test` geçti
- 20 yeni tablo tipi `types.ts`'e eklendi
- 7 yeni sayfa, 30+ yeni bileşen kopyalandı
- Tüm Lovable bağlantıları temizlendi

**Referans repoda 26 yeni migration var** (bizde olmayan).

---

## 🛡️ Kritik Kurallar

1. **Mevcut 45 migration dosyası DOKUNULMAYACAK**
2. **Mevcut tablolar SİLİNMEYECEK veya DEĞİŞTİRİLMEYECEK**
3. Sadece **yeni tablolar** ve **yeni kolonlar** eklenecek
4. Her migration `CREATE TABLE IF NOT EXISTS` / `ADD COLUMN IF NOT EXISTS` ile yazılacak
5. `handle_new_user()` fonksiyonu **mevcut haliyle korunacak** — referanstaki replacement ATLANACAK
6. Her batch sonrası **doğrulama** yapılacak

---

## Migration Analizi — 26 Dosya

### Batch 1: Güvenli — Sadece Yeni Tablolar (DOKUNMADIĞIMIZ tablolar)

| # | Migration | Açıklama | Yeni Tablolar |
|---|-----------|----------|---------------|
| 1 | `20260507102432` | Sosyal medya post arşivi | `generated_posts` + `post-archive` bucket |
| 2 | `20260507190822` | 19 Mayıs etkinlik başvuruları | `may19_submissions` + storage bucket |
| 3 | `20260510183120` | Platform mesajlaşma | `messages` + realtime |
| 4 | `20260511083015` | Sosyal cadde akışı | `feed_posts`, `feed_likes` |
| 5 | `20260511132859` | Cafe sistemi | `cafes`, `cafe_memberships` + feed_posts ALTER |
| 6 | `20260511145252` | Avatar storage bucket | `avatars` bucket |
| 7 | `20260511150748` | Kupon satın alma | `coupon_purchases` |
| 8 | `20260511153059` | İş ilanları | `job_listings` |
| 9 | `20260511173050` | Site ayarları (singleton) | `site_settings` |
| 10 | `20260512095045` | 1000 kurucu kayıtları | `founding_1000_signups` |
| 11 | `20260512095751` | İletişim mesajları | `contact_messages` |
| 12 | `20260512115822` | Kullanıcı bağlantıları | `user_connections` |
| 13 | `20260512122224` | Cafe kolonları + TR trigger kaldırma | `cafes` ALTER |

### Batch 2: Düşük Risk — Mevcut Tablolara Yeni Kolonlar

| # | Migration | Hedef Tablo | Yeni Kolonlar |
|---|-----------|-------------|---------------|
| 14 | `20260510094434` | `profiles` | `cv_name`, `cv_path`, `presentation_name`, `presentation_path` |
| 15 | `20260510110939` | `events` | `registration_url` |
| 16 | `20260511100503` | `profiles` | `profession`, `school` |
| 17 | `20260511142813` | `profiles` | `address`, `business_description`, `business_name`, `business_sector`, `business_website`, `city`, `country`, `presentation_name`, `presentation_path`, `show_on_map` + `profile_views` tablosu |
| 18 | `20260511151424` | `profiles` | `is_verified`, `hiring_mode` + `approval_requests` tablosu |
| 19 | `20260511161800` | `profiles` | `is_volunteer_mentor`, `mentor_topics`, `mentor_weekly_hours` |
| 20 | `20260512084757` | `profiles` | `phone_verified` + `phone_verifications` tablosu |
| 21 | `20260512100721` | `profiles` | `ambassador_referral_code`, `referral_discount_pct`, `referred_by_code` |

### Batch 3: Yüksek Risk — Mevcut Fonksiyonlara/Trigger'lara Dokunanlar

| # | Migration | Risk | Açıklama |
|---|-----------|------|----------|
| 22 | `20260511135018` | ORTA | Cafe kolonları + seed data + trigger (user_roles read-only) |
| 23 | `20260511171028` | YÜKSEK | `appointments` tablosu + notifications INSERT trigger |
| 24 | `20260512080857` | YÜKSEK | `notify_followers()` fonksiyonu + events/job_listings/cafes trigger |
| 25 | `20260512084007` | YÜKSEK | `job_applications` tablosu + notifications INSERT trigger |
| 26 | `20260512084757` | **TEHLİKELİ** | `handle_new_user()` fonksiyonunu DEĞİŞTİRİR — **ATLANACAK veya dikkatli uyarlanacak** |
| 27 | `20260512132508` | **TEHLİKELİ** | `on_auth_user_created` trigger + profile backfill — **ATLANACAK** |

---

## Uygulama Planı

### TODO-L0: Mevcut Backend Doğrulama
- [ ] Mevcut `supabase/migrations/` dizinindeki 45 dosyaya DOKUNMADIĞIMIZI doğrula
- [ ] `supabase/functions/` altındaki tüm edge function'ların değişmediğini doğrula
- [ ] Mevcut tablo listesini çıkar (psql veya Supabase Dashboard'dan)

### TODO-L1: Tek Bir Güvenli Migration Dosyası Oluştur

Tek bir `20260513000000_add_feed_and_social_tables.sql` dosyası oluştur. Bu dosya:

1. **Batch 1 — Yeni tablolar** (hepsini `CREATE TABLE IF NOT EXISTS` ile):
   - `generated_posts`
   - `may19_submissions`
   - `messages`
   - `feed_posts`
   - `feed_likes`
   - `cafes`
   - `cafe_memberships`
   - `coupon_purchases`
   - `job_listings`
   - `site_settings`
   - `founding_1000_signups`
   - `contact_messages`
   - `user_connections`
   - `profile_views`
   - `approval_requests`
   - `phone_verifications`
   - `user_follows`
   - `job_applications`
   - `appointments`

2. **Batch 2 — Yeni kolonlar** (hepsini `ADD COLUMN IF NOT EXISTS` ile):
   - `profiles` tablosuna: `cv_name`, `cv_path`, `presentation_name`, `presentation_path`, `profession`, `school`, `address`, `business_description`, `business_name`, `business_sector`, `business_website`, `city`, `country`, `show_on_map`, `is_verified`, `hiring_mode`, `is_volunteer_mentor`, `mentor_topics`, `mentor_weekly_hours`, `phone_verified`, `ambassador_referral_code`, `referral_discount_pct`, `referred_by_code`
   - `events` tablosuna: `registration_url`

3. **RLS policies** — her yeni tablo için `DO INSTEAD NOTHING` fallback ile

4. **Storage buckets** — `post-archive`, `avatars` (IF NOT EXISTS)

### TODO-L2: Dikkatli Yaklaşılacak Olanaklar (Ayrı Migration Dosyaları)

Her biri ayrı dosya ve ayrı onay:

- [ ] `20260513000001_add_notify_followers_function.sql` — `notify_followers()` fonksiyonu. Mevcut `notifications` tablosuna INSERT yapar → tablo yapısının uyumlu olduğunu doğrula
- [ ] `20260513000002_add_cafe_triggers_and_seed.sql` — Cafe trigger'ları + seed data
- [ ] `20260513000003_add_job_application_triggers.sql` — Job application notification trigger

### TODO-L3: ATLANACAK Migration'lar (Güvenlik Nedeniyle)

Bu migration'lar **KOPYALANMAYACAK** — mevcut `handle_new_user()` fonksiyonunu ve `on_auth_user_created` trigger'ını değiştiriyorlar:
- ❌ `20260512084757` — `handle_new_user()` değiştirir (phone logic ekler)
- ❌ `20260512132508` — `on_auth_user_created` trigger'ı yeniden oluşturur + backfill

Bunun yerine, bu iki migration'ın **gerekli kısmı** (`phone_verifications` tablosu ve `profiles` kolonları) L1'de zaten eklenecek. Sadece fonksiyon değişiklikleri atlanacak.

### TODO-L4: Migration Uygulama Sonrası Doğrulama

- [ ] Mevcut tabloların verilerinde kayıp yok
- [ ] RLS policy'ler çalışıyor
- [ ] Mevcut edge function'lar çalışıyor
- [ ] Frontend mevcut sayfaları (Radar, RegisterDiaspora vb.) sorunsuz
- [ ] Yeni sayfalar (Feed, JobBoard, May19 vb.) tablo hataları vermiyor
- [ ] `bun run build` hala hatasız

---

## Önerilen Uygulama Akışı

```
[L0] Mevcut backend doğrulama
  ↓
[L1] Tek güvenli migration dosyası oluştur (CREATE TABLE IF NOT EXISTS + ADD COLUMN IF NOT EXISTS)
  ↓ onay
[L1] Test ortamında uygula
  ↓ onay
[L2] Trigger migration'ları (ayrı ayrı onay)
  ↓ onay
[L4] Doğrulama
```

---

## Risk Matrisi

| İşlem | Risk | Azaltma Stratejisi |
|-------|------|-------------------|
| Yeni tablo oluşturma | YOK | `CREATE TABLE IF NOT EXISTS` |
| Yeni kolon ekleme | DÜŞÜK | `ADD COLUMN IF NOT EXISTS` |
| RLS policy ekleme | DÜŞÜK | Sadece yeni tablolar için |
| Storage bucket oluşturma | DÜŞÜK | `IF NOT EXISTS` |
| Trigger ekleme (notifications INSERT) | ORTA | Önce notifications tablo yapısını kontrol et |
| `handle_new_user()` değiştirme | **YÜKSEK** | ATLANACAK |
| Profile backfill | **YÜKSEK** | ATLANACAK |

---

## El ile Yapılacak Kontroller

1. **Supabase Dashboard → Table Editor**: `notifications` tablosunun `type`, `title`, `message`, `user_id`, `related_id` kolonlarına sahip olduğunu doğrula (trigger'lar bunlara INSERT yapacak)
2. **Supabase Dashboard → Storage**: Mevcut bucket'ları listele (`post-archive` zaten varsa atla)
3. **Supabase Dashboard → Database → Triggers**: `handle_new_user` trigger'ının mevcut yapısını kaydet (karşılaştırma için)

---

## Kaynak Dosyalar

Her migration dosyasının tam yolu:
```
referans/supabase/migrations/20260507102432_24b14704-2841-4e8d-b0e3-9bcf88587c24.sql
referans/supabase/migrations/20260507190822_14c18108-bbc7-4f87-9f5c-0b7b00d00eae.sql
... (26 dosya)
```

Uygulama sırasında her referans migration okunup `IF NOT EXISTS` ile güvenli hale getirilecek.
